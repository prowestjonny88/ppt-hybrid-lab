# Stage 4 Visual Oracle Findings v1

Status: visual ceiling generated; editable reconstruction pending.

## Benchmark authority

The generated full-slide images are visual-oracle evidence only. Frozen Semantic IR remains factual/text authority. Do not copy wording from the raster when it conflicts with Semantic IR, and do not count a flattened oracle image placed on a slide as editable-system success.

## Cross-slide visual findings

1. The oracle family is materially more authored than the current native Stage 4 direction: stronger focal hierarchy, deeper foreground/background separation, fewer generic rectangular UI surfaces, and more purposeful asymmetry.
2. Each slide has one unmistakable visual protagonist and uses supporting content as composition rather than equal-weight cards.
3. Complex visual texture is concentrated in a bounded hero region while explanatory text remains comparatively simple. This suggests a hybrid reconstruction strategy: native semantic text + native structural geometry + replaceable high-complexity visual assets.
4. Visual rhythm changes meaningfully by slide instead of repeating one template grammar.

## problem-hook oracle

Observed direction:
- dark editorial canvas;
- very large headline and dominant gradient wait-time metric on the left;
- expressive human image on the right with restrained technical linework;
- supporting copy is lower contrast and subordinate;
- strong emotional protagonist rather than a documentary queue photograph.

Reconstruction target:
- keep headline, metric, annotation and source as native PowerPoint text;
- use native rules/frames for the technical linework where practical;
- use a replaceable generated or benchmark visual asset only for the human/scene hero region;
- do not rasterize the left text field.

## how-it-works oracle

Observed direction:
- clean light canvas;
- large diagram/illustration dominates the left ~60%;
- explanatory headline and summary sit on the right;
- process is expressed as a continuous visual system rather than four text labels connected by generic lines;
- layered depth, isometric devices and routed signal lines provide the visual punch.

Important semantic defect in the oracle raster:
- the generated headline visibly duplicates the word "feed". This is direct evidence that raster text must not become semantic authority.

Reconstruction target:
- retain all authoritative headline/subtitle/source text as native text from Semantic IR;
- realize the left process as one replaceable high-complexity illustration asset OR a mixed native/SVG diagram, but keep semantic process labels represented in the realization ledger;
- avoid reproducing the oracle's duplicated wording;
- preserve a meaningful editable path for core nodes/labels even when illustration texture is raster/SVG.

## validation-traction oracle

Observed direction:
- light canvas with a large title band;
- a dark sculptural evidence field occupies the left half;
- metrics are integrated into that field rather than separated into KPI cards;
- multiple evidence strands converge visually toward a single next-proof destination on the right;
- the composition turns evidence into an argument, not a dashboard.

Reconstruction target:
- keep all metrics, labels, pilot destination and source as native semantic text;
- use native lines/curves/geometry or a replaceable abstract evidence-strand visual behind/around native metrics;
- maintain the distinction between promising intent/accuracy evidence and the still-unproven pilot gate;
- avoid equal-weight metric cards.

## Immediate implementation hypothesis

The quality gap is currently concentrated above the PPTX object renderer: visual direction, composition solving and complex-asset realization. The existing renderer lanes remain useful. For the first reconstruction pass, do not rewrite the PowerPoint substrate. Instead add oracle-derived composition variants and allow a bounded replaceable-visual lane for high-complexity hero artwork while preserving native semantic text, provenance, capacity checks, and round-trip identity.

## Next gate

Produce a three-slide `oracle_reconstruction_v1` deck using the same frozen Semantic IR. Render it through the fixed LibreOffice pipeline and compare:

- native editable baseline;
- generated oracle ceiling;
- hybrid editable reconstruction.

The reconstruction only qualifies if semantic text remains authoritative/editable, no full-slide oracle raster is used as a slide background shortcut, provenance/round-trip checks remain green, and human visual comparison shows a meaningful reduction in the visual-quality gap.
